export type ItemType = {
    name: string,
    imgUrl: string,
    category: string,
    price: number
}

export type CartCardPropType = {
    name: string
    imgUrl: string,
    price: number,
    quantity: number,
}